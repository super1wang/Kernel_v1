# 内核开发约束

## 授权和范围

- 目标架构为根目录 `Portable_Command_Runtime_Final_Architecture_v1.0.md`。用户请求优先于文档中的开发建议。
- 本阶段只建设内核及验证内核所需的最小消费者，不实现 CAD、CAM、Machine、Process、Qt UI 或其他产品模块。
- 用户已明确授权必要节点的本地提交及推送到 `https://github.com/super1wang/Kernel_v1`；正常快进提交、推送无需再次确认。禁止强推、覆写远端历史或修改原内核工作区。
- 原内核仅作参考。固定结构参考 `52b099a9e9cabfafe5a6c2c3e95f92c2e857ed03`，行为与修复参考 `432434f9c43d3f83c124ac1cf23fec39018f394f`，不从原仓库当前 HEAD 隐式取代码。
- `.reference/` 仅存固定版本的隔离参考材料，不修改其中源文件。构建、数据库、日志与 IPC 名称只使用本工作区独立路径。

## 结构与行为

- Foundation、Contracts、CoreRuntime 不依赖 Workspace、Document、Project、领域修订、Qt、OCCT、SQL 实现和第三方公开类型。
- 静态组件按需构建、安装、链接和启动，显式 Host 装配；先验证依赖 DAG，再注册、准备和冻结；不热卸载。
- 新建 Host、ModuleContext 与受治理执行边界，不整包搬入 AppKernel 或集中 PersistenceService，不合并 donor。
- 迁移单位包含接口、实现、测试和行为约束；承接 donor 的线程池排空、析构屏障、持久 Host 独占等修复。
- 统一网关保留输入预算、主体认证、授权复核、目标/资源租约、取消和真实执行事实。不得用假文档、空成功、void* 或任意字典掩盖缺失实现。
- Script/Plan 共用一套 AST。顺序步骤保留独立提交事实；AtomicGroup 由兼容状态提供者治理，子操作不持有提交权限。
- 算法内部直接类型化调用，不逐函数进入命令系统；观察失败、迟到取消不能抹掉已提交或外部动作事实。
- stop 超时保留依赖与未完成清单；不 detach 后释放对象，不从 worker 销毁所属 Host。

## 检查点和证据

- 详细状态只维护 `docs/执行计划与测试映射.md`；交付报告是时间点证据，不另建竞争进度表。所有开发文档使用中文。
- 每次推进一个可验证检查点。文件迁移、语义修改、第三方升级和优化分别提交。
- 先建立正负样例，再替换执行链。扫描覆盖公开头、私有头及实现文件；安装树消费者验证依赖裁剪。
- 每个检查点记录实际命令、退出码、完成标记、来源与二进制摘要、未运行项和性能证据；旧测试通过不代表新内核通过。
- 首批推进 M0 至 M3，M3 作继续/调整决策点；不提前声称 M4 至 M8 已实现或 Kernel Frozen。
- 所有测试数据隔离；不清理用户材料或删除恢复故障样本。性能不能以减少授权、校验、耐久性或寿命保证换取。
