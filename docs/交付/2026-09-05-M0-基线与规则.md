# M0：基线与规则交付

日期：2026-09-05。状态：M0.1 固定来源与规则门禁已通过；M0.2 参考回归/性能验证进行中，M0 尚未整体关闭。

## 本次交付

新仓库沿用远端初始历史。用户架构原文已保留；新增中文 ADR、开发约束、唯一执行计划及行为测试映射。原内核仅作固定参考，没有全量迁移 AppKernel、领域模块或数据库实现。

结构参考 `52b099a9...` 共 67 个归档文件，donor `432434f9...` 共 362 个归档文件；五个 Git 依赖与 SQLite 下载摘要均已验证。参考源及依赖位于忽略的 `.reference/`，原工作区没有修改。完整文件摘要见 `../evidence/m0/source-manifest.json`。

边界门禁覆盖生产公开头、私有头、内联实现及 C/C++ 源码；拒绝 Core 反向引用状态扩展、第三方类型泄漏、旧内核回接、公开头引用私有实现及未知组件。当前没有生产代码，扫描会明确声明这一事实，不能作为 M1 编译/安装证明。15 个故意违规的边界样例和证据失败样例验证门禁不会空报成功。

## 运行记录

| 项目 | 命令/证据 | 实际结果 |
|---|---|---|
| 固定版本与依赖 | `python tools/prepare_baseline.py --repository J:/Code/LaserCNCv3.0 --dependency-cache J:/Code/LaserCNCv3.0/build/vs2022/_deps` | 退出 0，`reference-verified` |
| donor 配置 | `../evidence/m0/donor-configure.json` | 退出 0，MSVC 19.44.35216.0、Windows SDK 10.0.26100.0 |
| donor Release 构建 | `cmake --build build/donor-release --config Release --parallel 16` | 退出 0；完整记录见 `../evidence/m0/donor-build.*` |
| 新仓库架构/证据门禁 | `ctest --test-dir build/m0 -C Release --output-on-failure` | 退出 0，3/3；见 `../evidence/m0/architecture-final.*` |
| donor Release 回归 | `ctest --test-dir build/donor-release -C Release --output-on-failure` | 进行中 |
| 初始性能矩阵 | `python tools/run_baseline.py --runs 3 --samples 64` | 等待回归完成后运行，避免负载干扰 |

首次 M0 CTest 调用遗漏多配置生成器的 `-C`，测试显示 Not Run、退出非零；已补配置参数后重新运行。最初核对 Catch2 时比较了标签对象与提交对象，解引用后证实同一版本，现已全部改用精确对象导出的隔离依赖并重新构建。没有修改或升级依赖。

## 剩余边界

本检查点尚无新 C++ 运行时。参考 Release 回归和初始基线不替代新内核准入。计划中的足量 P99、新旧等价 A/B、1/4/16 客户端、任务阶段开销、10k/100k 状态及容量、Debug/ASan、安装重定位和两个真实消费者仍待相应节点验证。

下一检查点为 M1 可裁剪骨架：按固定来源复用 Foundation，建立独立 Contracts/CoreRuntime 与按项适配器、显式 Host/ModuleContext、组件依赖图和安装消费者。禁止通过假文档、空成功适配器或提前迁移领域来补缺。
