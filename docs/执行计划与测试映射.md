# 执行计划与测试映射

依据：根目录最终架构 v1.0。最后更新：2026-09-05。

## 当前检查点

M0 正在实施。M0.1 固定来源、完整边界扫描与负例、复现脚本和 donor 隔离 Release 构建已完成；M0.2 的 407 项参考回归及初始性能基线正在验证。新运行时尚未实现，未 Frozen。

| 节点 | 状态 | 本节点交付及退出证据 |
|---|---|---|
| M0 | 进行中 | 固定参考版本、来源/行为映射、私有头扫描负例、当前语义 Release 测量、ADR |
| M1 | 待实施 | Foundation/Contracts、按项 adapter、Host/ModuleContext、DAG、安装最小消费者；无 Workspace/SQLite，缺组件失败 |
| M2 | 待实施 | Command/Query、身份/授权、Schema、固定绑定、CommandSpec/Docs、发现、事件/日志；拒绝路径不调用 handler |
| M3 | 待实施 | 无文档任务、资源、取消、进度/结果、容量、停止/排空及 owner；竞态验证与第一轮性能比较 |
| M4 | 待实施 | 同一 AST 的顺序计划、结果引用、临时计划、预算；部分失败保留前步事实 |
| M5 | 待实施 | Durable、幂等、Task/Effect、独占/恢复；无 Workspace 表，不盲重放 |
| M6 | 待实施 | Workspace、类型化目标、事务/History/生命周期、原子组、旧数据与提交回执原子性 |
| M7 | 待实施 | Workflow、Named Pipe/CLI、任务/日志控制、AI 合同测试；多进程与断线/权限/背压 |
| M8 | 待实施 | 正式 Profile、重定位安装、三配置、崩溃/容量/性能、两种状态需求消费者；最终冻结审计 |

## 行为与验证责任

| 编号 | 必须保留的行为 | 首次准入节点 | 必需验证 |
|---|---|---|---|
| B01 | Core 无状态/领域/第三方耦合，私有头亦受约束 | M0/M1 | 扫描正负例、安装树独立消费者、缺组件配置失败 |
| B02 | 注册失败不可吞、DAG 先验证、Ready 后不可变更 | M1/M2 | 重复/缺失/环/准备失败逆序清理、冻结后注册 |
| B03 | 可信主体、会话分离、未知命令/版本/参数/权限拒绝 | M2 | handler 调用计数为零、权限撤销、跨主体拒绝 |
| B04 | 输出错误与日志失败不反转事实 | M2/M5/M6 | ReadOnly 拒绝无效输出；写前验证；动作后 Applied/Indeterminate |
| B05 | 资源原子获取、共享/独占同槽、溢出/公平/别名治理 | M3 | 冲突、取消、饱和、未知资源、极值/租约析构 |
| B06 | 接受、计算、应用、终态发布、执行器排空可区分 | M3 | 完成回调阻塞、迟到取消、stop 超时及最终析构进程探针 |
| B07 | 顺序前步提交不能被后步失败抹去 | M4 | 停止/预算/引用错/未知步骤及实值 Schema 检查 |
| B08 | durable claim 在副作用之前，恢复不重放未知动作 | M5 | 独占双进程、崩溃窗口、重连、幂等键与主体隔离 |
| B09 | 原子组无子提交权限、失败不发布、提交前授权复核 | M1/M2 合同，M6 实现 | 无提供者拒绝、类型约束、组内失败/过期/权限/存储失败 |
| B10 | 旧数据不静默删库、不重置非零修订、不丢故障样本 | M6 | 精确 reader/显式迁移/拒绝及损坏恢复材料 |
| B11 | 本地 IPC 认证/预算，控制请求独立容量 | M7 | 两 CLI 同实例、断线与慢客户端、猜 ID/跨主体/取消 |
| B12 | 观测有界、失败隔离、必要审计不能丢 | M2/M3/M5 | 慢/异常 exporter、容量溢出、审计失败 |
| B13 | 性能不弱化保障；真实裁剪与唯一核心 | M3/M8 | 固定源/二进制/配置、交错多进程与足量分位样本、重定位和组件矩阵 |

## 验证状态规则

`通过` 必须引用本工作区实际运行证据；`待实施`、`未运行`、`已知缺口` 分开记录。原内核测试只标记为参考版本验证。M3 是首批开发决策点，未通过前不扩大到 Workspace 或产品领域。文档数量和 CTest 数量都不等于最终准入。
